<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.0</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.honghu.ai</groupId>
    <artifactId>honghu-ai</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>honghu-ai</name>
    <description>Honghu AI — 企业级多模型智能对话与 RAG 平台 (Spring Boot 3 + Spring AI)</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>17</java.version>
        <!--
            Spring AI 统一版本号。

            当前升级到 1.0.0 GA，目的是使用稳定版 ToolCallback、ChatClient、OpenAI-compatible
            和 Milvus starter API；所有 Spring AI 依赖都引用这个属性，避免不同模块版本不一致。
        -->
        <spring-ai.version>1.0.0</spring-ai.version>
    </properties>

    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>spring-snapshots</id>
            <name>Spring Snapshots</name>
            <url>https://repo.spring.io/snapshot</url>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
    </repositories>
    <dependencies>
        <!-- Spring Boot Web Starter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!--
            Spring Security（P0-1：框架级认证与授权）。

            之前身份完全靠 X-User-Id 请求头 + 内存 opaque token，属于“前端说自己是谁就是谁”。
            引入正式 Spring Security 过滤链后：
            - 前台用户态统一由签名 JWT 驱动（见 security/jwt），无法被前端伪造；
            - 管理端继续用各自的会话校验（AdminAuthService），互不影响；
            - 通过 @EnableMethodSecurity 支持方法级 RBAC（如用户列表只允许 ADMIN）。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!--
            Actuator + Micrometer Prometheus（P0-2：可观测性）。

            actuator 暴露 /actuator/health、/actuator/prometheus；
            micrometer-registry-prometheus 负责把 JVM、http.server.requests 以及
            我们自定义的网关业务指标（路由比例、token、成本、时延）转成 Prometheus 文本格式。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!--
            JJWT（JSON Web Token 签发与校验）。

            api 为编译期接口，impl / jackson 为运行期实现，按官方推荐拆三个坐标引入。
            用 HS256 + 服务端密钥签名，保证 token 一旦签发就无法被客户端篡改 userId / role。
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.5</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.5</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.5</version>
            <scope>runtime</scope>
        </dependency>

        <!--
            Spring AI Ollama Starter。

            这个 starter 负责本地 Ollama/DeepSeek 测试模型的 Spring AI 集成。
            升级 1.0.0 后 artifactId 使用 starter-model-ollama，和旧版命名不同。
        -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-ollama</artifactId>
            <version>${spring-ai.version}</version>
        </dependency>

        <!--
            Spring AI OpenAI Starter。

            当前主要用于 RAG embedding 的 OpenAI-compatible 客户端能力；
            Chat 调用不直接依赖默认 OpenAiChatModel，而是由 AiChatModelGatewayService 路由到 DeepSeek/Ollama 等 provider。
        -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
            <version>${spring-ai.version}</version>
        </dependency>

        <!--
            Spring AI Milvus Vector Store。

            检索侧仍使用 Spring AI VectorStore 做 similaritySearch；
            写入侧有自定义 MilvusVectorRepository 负责幂等 delete/insert，两者共享同一套 Milvus 配置。
        -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-vector-store-milvus</artifactId>
            <version>${spring-ai.version}</version>
        </dependency>

        <!-- Spring Boot Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        
        <!-- Spring Security Crypto (密码加密) -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>
        
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        
        <!-- SpringDoc OpenAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.2.0</version>
        </dependency>
        
        <!-- OkHttp (保留原有依赖) -->
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
            <version>4.12.0</version>
        </dependency>

        <!--
            Fastjson。

            Milvus Java SDK 的 JSON 字段插入当前使用 com.alibaba.fastjson.JSONObject；
            这里保留该依赖，避免 RAG 向量写入 metadata JSON 时缺少运行时类型。
        -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>fastjson</artifactId>
            <version>1.2.83</version>
        </dependency>

        <!-- Spring Boot Data JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
            <version>3.2.0</version> <!-- 显式指定版本以确保兼容 -->
        </dependency>

        <!-- Spring Boot Redis（用于基于 Token 的短期记忆滑动窗口） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>


        <!-- JTokkit，用于按 token 估算上下文窗口 -->
        <dependency>
            <groupId>com.knuddels</groupId>
            <artifactId>jtokkit</artifactId>
            <version>1.1.0</version>
        </dependency>

        <!-- Spring Boot AOP Starter (for @Transactional) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>

        <!-- PostgreSQL Driver -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Liquibase -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- ========================= -->
        <!-- AWS SDK v2（模块化按需引入） -->
        <!-- v2 是 AWS 官方推荐版本，相比 v1：                -->
        <!--   - 完全模块化，按需引入，体积更小              -->
        <!--   - 原生支持异步（Netty NIO）                   -->
        <!--   - 不可变模型 + Builder 模式，线程安全          -->
        <!--   - 持续维护，v1 已进入仅修 Bug 模式            -->
        <!-- ========================= -->

        <!-- AWS S3（对象存储：文件上传、预签名 URL） -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <version>2.25.27</version>
        </dependency>

        <!-- AWS SQS（消息队列：异步任务、事件驱动） -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>sqs</artifactId>
            <version>2.25.27</version>
        </dependency>

        <!-- Spring Cloud AWS SQS：提供 @SqsListener 注解式消息消费能力 -->
        <dependency>
            <groupId>io.awspring.cloud</groupId>
            <artifactId>spring-cloud-aws-starter-sqs</artifactId>
            <version>3.1.0</version>
        </dependency>

        <!-- PDFBox：第一版简单 RAG 先支持 PDF / 纯文本文件解析 -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>3.0.2</version>
        </dependency>

        <!-- Apache POI OOXML：支持 DOCX 文档文本提取 -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>5.2.5</version>
        </dependency>

        <!-- Test Dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Spring Security 测试支持（@WithMockUser、SecurityMockMvc 等），仅测试期使用 -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <compilerArgs>
                        <!--
                            保留 Java 方法参数名。

                            @NativeTool / BuiltinToolIntrospector 需要读取方法参数名生成 JSON Schema；
                            如果没有 -parameters，反射只能拿到 arg0、arg1，模型看到的工具参数会变得不可读。
                        -->
                        <arg>-parameters</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <mainClass>com.honghu.ai.assigment.service.AiTest</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            CI profile: run only the infrastructure-free unit tests.

            The full @SpringBootTest context (and the manual Ollama-tunnel
            runners) need PostgreSQL / Redis / Milvus / a live model endpoint,
            which CI does not provision. Everything else is Mockito-based and
            runs anywhere. Activate with: mvn -Pci verify
        -->
        <profile>
            <id>ci</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <excludes>
                                <exclude>**/HonghuAiApplicationTests.java</exclude>
                                <exclude>**/ConnectionExceptionTest.java</exclude>
                                <exclude>**/ConnectionTest.java</exclude>
                                <exclude>**/AiTest.java</exclude>
                            </excludes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
